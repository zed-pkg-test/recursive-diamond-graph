[package]
org = "zed-pkg-test"
name = "recursive-diamond-graph"
version = "0.1.0"
description = "Independent package acceptance harness"
license = "MIT"

[package.repository]
vcs = "git"
url = "https://github.com/zed-pkg-test/recursive-diamond-graph"

[install]
dir = ".vendor/.zed"

[dependencies]
"zed/zed-cli" = "*"

[publish]
exclude = ["artifacts/**", "vendor/**", ".vendor/**", ".env", ".env.*"]
